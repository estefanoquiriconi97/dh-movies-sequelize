<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detalles del Actor</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/detail.css" />
  </head>

  <body class="container">
    <figure class="logo">
      <a href="/">
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </a>
    </figure>
    <h1 class="heading">Actor detail</h1>

    <% if (actor) { %>
    <div class="details">
      <h2><%= actor.first_name %> <%= actor.last_name %></h2>
      <p>Rating: <%= actor.rating %></p>
      <div class="button-container">
        <form action="/actors/delete/<%= actor.id %>?_method=DELETE" method="post">
            <button class="action-button">Delete</button>
        </form>
        <br>
        <a href="/actors/edit/<%= actor.id %>" class="action-button">Edit</a>
        <br>
        <br>
        <a href="/actors" class="action-button">Back</a>
    </div>
    
    </div>
    <% } else { %>
    <p>No se encontraron detalles para este actor.</p>
    <a href="/actors">Volver al listado de actores</a>
    <% } %>
  </body>
</html>
